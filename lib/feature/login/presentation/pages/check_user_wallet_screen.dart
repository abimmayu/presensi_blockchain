import 'package:flutter/material.dart';

class IntializeUser extends StatelessWidget {
  const IntializeUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
